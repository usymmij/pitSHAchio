{
  "dependencies": {
    "express": "^4.19.2",
    "js-sha256": "^0.11.0",
    "net": "^1.0.2",
    "nodemon": "^3.1.0"
  },
  "scripts": {
    "start": "nodemon index.js",
    "build": "cd client && npm install && npm run build"
  }
}
